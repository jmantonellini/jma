name: Deploy to CapRover (no GHCR)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Deploy to CapRover
        uses: caprover/deploy-from-github@v1.1.2
        with:
          server: https://eco-sistema.net
          app: jma
          token: ${{ secrets.APP_TOKEN }}